import React from 'react';

// This component is no longer in use and has been replaced by LeaderboardView.
// It is kept to avoid breaking imports if referenced somewhere unexpectedly,
// but its content has been removed.
const DashboardView: React.FC = () => {
    return null;
};
export default DashboardView;